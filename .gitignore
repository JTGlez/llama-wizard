# Local Pi runtime state
.atl/
.pi/
models/*.gguf
src/llama.cpp/

# Docker build context (staged binarios + .so from the host build)
build-context/

# Downloaded installers (CUDA .run, etc.)
*.run

# Downloaded installers (CUDA .run, etc.)
*.run
